# Goose 数据库迁移工具配置
# 使用方式：goose -env .example.env up
# 或者设置环境变量后直接使用：goose up

# 数据库驱动类型（postgres, mysql, sqlite3, mssql 等）
GOOSE_DRIVER=mysql

# MySQL 数据库连接字符串
# 格式：user:password@tcp(host:port)/dbname?parseTime=true&loc=Local
# Docker 环境：使用服务名 mysql，端口 3306（容器内端口）
# 本地开发：使用 localhost，端口 4267（映射端口）
GOOSE_DBSTRING=root:zhkj2580@tcp(192.168.11.71:3306)/bid_engine?parseTime=true&loc=Local

# 迁移文件目录（可选，默认当前目录）
# GOOSE_MIGRATION_DIR=.

# 迁移表名（可选，默认 goose_db_version）
# GOOSE_TABLE=goose_db_version
